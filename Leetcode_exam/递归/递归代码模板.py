"""
Editor:Hyean
E-mail:1067065568@qq.com
"""


def recursion(level, *args):
    """ recursion terminator  终止递归循环的条件"""
    # if level > Max_level:
    #     process result
    #     return

    """process logic in current level    在当前这一层需要处理的逻辑操作"""
    # process(level, data……)
    """ drill down    调用函数自身，进行递归"""
    # self.recursion(level+1, *args)
    """reverse the current level status if needed"""
    pass
